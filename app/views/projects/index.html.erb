<div class="white-space"></div>
<div class="projects-index">
  <div class="projects-index_filters">
    <hr align="left">
    <h4>Take a look at the projects we've built</h4>
    <p>From mobile apps to tailored CMS' (Content Management Systems), our works are a showcase of the different challenges and technologies our team has learned since the beginning of our operations.</p>
  </div>
  <div class="projects-index_container">
    <div class="projects-index_cards">
      <% @projects.each do |project| %>
        <%= link_to project_path(project) do %>
          <div class="projects-index_card">
            <div class="projects-index_card_image">
              <% if project.images.attached? %>
                <%= image_tag project.images.first %>
              <% else %>
                <%= image_tag 'https://images.pexels.com/photos/356079/pexels-photo-356079.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940' %>
              <% end %>
            </div>
            <div class="projects-index_card_content">
              <h6><%= project.title %></h6>
              <div class="projects-index_card_content_description">
                <p>Project Type:</p>
                <p style=" <% if project.project_type == "Mobile App" || project.project_type == "Mobile Application" %>
                  background-color: #673AB7;
                  <% elsif project.project_type == "Web App" || project.project_type == "Web Application"%>
                  background-color: #546E7A;
                  <% elsif project.project_type == "Website" %>
                  background-color: #D81B60;
                  <% elsif project.project_type == "Progressive App" || project.project_type == "Progressive Web App" %>
                  background-color: #FFC400;
                <% end %>">
                  <%= project.project_type %>
                </p>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
